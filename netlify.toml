[build]
  command = "npm run build"
  publish = "dist"

# Set Node version for Netlify (React 19 needs Node 18+)
[context.production.environment]
  NODE_VERSION = "20"

# Redirect all routes to index.html for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
