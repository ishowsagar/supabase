# Example environment variables for local development
# Copy this file to `.env` and replace the placeholders with your real Supabase values.
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_KEY=your-public-anon-key

# NOTE: Use the anon/public key for client-side apps. Do NOT commit real secrets.